# devenv shells are built impurely, so this needs --impure. It works with plain
# direnv + nix; the devenv CLI is not required.
#
# If you do have the devenv CLI and would rather use its direnv integration,
# swap the line below for `use devenv`.
use flake . --impure
